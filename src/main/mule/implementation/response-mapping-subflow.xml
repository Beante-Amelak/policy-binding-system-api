<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">

    <!-- =========================================================
         Response Mapping Subflow
         Normalizes PAS response to PolicyBindingResponse schema
         Ensures consistent API response format
         ========================================================= -->
    <sub-flow name="response-mapping-subflow" doc:name="Response Mapping Subflow">
        
        <!-- Log Response Mapping Start -->
        <logger level="DEBUG" 
                doc:name="Log Response Mapping Start" 
                message="#[output application/json --- {logType: 'DEBUG', correlationId: vars.correlationId, message: 'Starting response mapping'}]"/>

        <!-- Transform PAS Response to PolicyBindingResponse -->
        <ee:transform doc:name="Transform to PolicyBindingResponse">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    policyNumber: vars.pasResponse.policyNumber default "",
    policyId: vars.pasResponse.policyId default vars.pasResponse.id default "",
    status: "BOUND",
    effectiveDate: vars.originalRequest.effectiveDate as String,
    message: "Policy successfully bound"
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <!-- Log Response Mapping Complete -->
        <logger level="DEBUG" 
                doc:name="Log Response Mapping Complete" 
                message="#[output application/json --- {logType: 'DEBUG', correlationId: vars.correlationId, message: 'Response mapping completed', policyNumber: payload.policyNumber}]"/>

    </sub-flow>

</mule>
